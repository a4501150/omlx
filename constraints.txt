# Pip constraints mirroring [tool.uv] override-dependencies in pyproject.toml.
#
# mlx-audio transitively pins mlx-lm==0.31.1, which collides with our main
# dependency on mlx-lm from a newer git commit. uv respects the override in
# pyproject.toml automatically; pip does not, so pip users must pass
#   pip install -c constraints.txt -e '.[dev,mcp,audio]'
# to force the resolver to use our pinned mlx-lm commit everywhere.
#
# Keep this in sync with pyproject.toml's `mlx-lm` line.
mlx-lm @ git+https://github.com/ml-explore/mlx-lm@ed1fca4cef15a824c5f1702c80f70b4cffc8e4dd
